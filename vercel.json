{
  "version": 2,
  "builds": [
    {
      "src": "app/main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app/main:app"
    }
  ],
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "echo 'No build step required'"
}
